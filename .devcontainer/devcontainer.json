{
  "name": "Spotter (Sail)",
  "dockerComposeFile": ["../docker-compose.yml"],        // only the main compose is needed
  "service": "laravel.test",
  "workspaceFolder": "/var/www/html",                    // <- KEY: use Sail's mount
  "remoteUser": "sail",                                  // optional, nice to have
  "overrideCommand": true,
  "customizations": {
    "vscode": {
      "extensions": [
        "bmewburn.vscode-intelephense-client",
        "xdebug.php-pack"
      ]
    }
  }
}