name: Validation Pipeline

on:
    pull_request:
        branches:
            - main
            - prod
    push:
        branches:
            - tests/*

permissions:
    contents: read

jobs:
    build:
        runs-on: ubuntu-24.04

        steps:
            - name: Pull the current branch
              uses: actions/checkout@v7

            - name: Inspect current repo
              run: |
                  pwd
                  ls -la

            - name: Run the docker image
              run: |
                  docker build -t spotacus:latest .
                  docker images

            # checkout the repo current branch

            # build the image

            # login to a registry

            # push it to a registry

            # get the tags to identify the build image

    # test:
    # login to a registry again

    # pull the image

    # run it

    # start testing it
