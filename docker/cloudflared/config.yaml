# No "tunnel:" line here when using a token
ingress:
  - hostname: mysql.spotacus.app
    service: tcp://mysql:3306   # requires cloudflared + mysql on same Docker network

  - service: http_status:404    # catch-all MUST be last